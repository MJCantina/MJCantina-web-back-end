// Espera até que o DOM esteja totalmente carregado
document.addEventListener("DOMContentLoaded", function() {
    // Pegue o elemento com o ID 'sair'
    const btnSair = document.getElementById('SAIR');
    
    // Adiciona o evento de clique no botão 'Sair'
    btnSair.addEventListener('click', function() {
        // Redireciona para outra página
        // Aqui, você pode colocar o link que deseja. Exemplo:
        window.location.href = "./login.html";  // Redireciona para a página de logout
        
        // Caso queira abrir em uma nova aba:
        // window.open('logout.html', '_blank');
    });
});


// Espera até que o DOM esteja totalmente carregado
document.addEventListener("DOMContentLoaded", function() {
    // Pegue o elemento com o ID 'sair'
    const btnSair = document.getElementById('FUNCIONARIOS');
    
    // Adiciona o evento de clique no botão 'Sair'
    btnSair.addEventListener('click', function() {
        // Redireciona para outra página
        // Aqui, você pode colocar o link que deseja. Exemplo:
        window.location.href = "./funcionarios.html";  // Redireciona para a página de logout
        
        // Caso queira abrir em uma nova aba:
        // window.open('logout.html', '_blank');
    });
});

// Espera até que o DOM esteja totalmente carregado
document.addEventListener("DOMContentLoaded", function() {
    // Pegue o elemento com o ID 'sair'
    const btnSair = document.getElementById('VALORES');
    
    // Adiciona o evento de clique no botão 'Sair'
    btnSair.addEventListener('click', function() {
        // Redireciona para outra página
        // Aqui, você pode colocar o link que deseja. Exemplo:
        window.location.href = "./estoque.html";  // Redireciona para a página de logout
        
        // Caso queira abrir em uma nova aba:
        // window.open('logout.html', '_blank');
    });
});


// Espera até que o DOM esteja totalmente carregado
document.addEventListener("DOMContentLoaded", function() {
    // Pegue o elemento com o ID 'sair'
    const btnSair = document.getElementById('GERAIS');
    
    // Adiciona o evento de clique no botão 'Sair'
    btnSair.addEventListener('click', function() {
        // Redireciona para outra página
        // Aqui, você pode colocar o link que deseja. Exemplo:
        window.location.href = "./telainicial.html";  // Redireciona para a página de logout
        
        // Caso queira abrir em uma nova aba:
        // window.open('logout.html', '_blank');
    });
});
